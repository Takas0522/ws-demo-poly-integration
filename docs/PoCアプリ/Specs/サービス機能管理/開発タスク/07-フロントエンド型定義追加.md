# 07 — フロントエンド型定義追加

## 概要

サービス機能管理のフロントエンドで使用する TypeScript 型定義を `types/index.ts` に追加する。

## 背景・目的

BFF 経由で取得するサービス機能データの型安全性を確保するため、`ServiceFeature` と `TenantServiceFeature` のインターフェースをフロントエンドの型定義ファイルに追加する。

## 対象リポジトリ

- `src/front/` — 型定義ファイルの変更

## タスク詳細

### 実装内容

1. **`src/front/types/index.ts` への型定義追加**

   以下のインターフェースを追加する:

   ```typescript
   /** サービス機能マスター */
   export interface ServiceFeature {
     id: string;
     service_id: string;
     feature_key: string;
     feature_name: string;
     description: string;
     default_enabled: boolean;
     created_at: string;
   }

   /** テナント別機能設定（マスターとマージ済みレスポンス） */
   export interface TenantServiceFeature {
     feature_id: string;
     service_id: string;
     feature_key: string;
     feature_name: string;
     description: string;
     is_enabled: boolean;
     is_default: boolean;
     updated_at: string | null;
     updated_by: string | null;
   }
   ```

### 参照ドキュメント

- [04-API仕様.md](../04-API仕様.md) — セクション 4.1（TypeScript 型定義）
- 既存コード: `src/front/types/index.ts`

### 影響範囲

- `src/front/types/index.ts` — インターフェース追加（既存の型定義は変更なし）

## 完了条件（Success Criteria）

- [ ] `src/front/types/index.ts` に `ServiceFeature` インターフェースが定義されている
- [ ] `src/front/types/index.ts` に `TenantServiceFeature` インターフェースが定義されている
- [ ] 各フィールド名が API レスポンスの snake_case フィールドと一致している
- [ ] `npm run build` がエラーなく完了する
- [ ] 既存の型定義（`User`, `Role`, `Tenant`, `Service`, `TenantService` 等）が変更されていない

## 依存関係

- **前提タスク:** なし（タスク01〜04と並行実施可能）
- **後続タスク:** 08-フロントエンドUI実装

## 備考

- API レスポンスは snake_case（バックエンドの Python/Pydantic に準拠）であるため、フロントエンドの型定義もそのまま snake_case を使用する（既存の BFF パターンに合わせる）
